//
//  Newest_DemoApp.swift
//  Newest Demo
//
//  Created by Mansa Patel on 2/8/25.
//

import SwiftUI

// The entry point of the SwiftUI app. This is where the app’s life begins.
@main

 // The body property defines the content and behavior of the app's window.
struct Newest_DemoApp: App {
    var body: some Scene {
        // Create a new `WindowGroup` scene, which is responsible for managing the app's window.
        WindowGroup {
            // The root view of the app. `ContentView()` is the initial view that will be displayed in the window.
            ContentView()
        }
    }
}
